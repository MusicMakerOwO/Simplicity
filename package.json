{
  "name": "simplicity",
  "version": "0.0.1",
  "description": "What discord.js could have been",
  "main": "index.js",
  "author": "@musicmaker",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node ./pre-build.js && echo \"Compiling to JS...\" && tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "mpg123-decoder": "^1.0.0",
    "node-wav": "^0.0.2"
  }
}
