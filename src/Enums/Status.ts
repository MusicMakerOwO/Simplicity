export enum Status {
    Playing = 0,
    Streaming = 1,
    Listening = 2,
    Watching = 3,
    Custom = 4,
    Competing = 5
} 

export enum Pressence { 
	ONLINE = 'online',
    DND = 'dnd',
	IDLE = 'idle',
	OFFLINE = 'offline',
	INVISIBLE = 'offline'
}